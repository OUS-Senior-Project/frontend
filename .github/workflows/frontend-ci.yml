name: Frontend CI (temporary)

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  lint_and_test_frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # - name: Set up Node
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '18'

      # - name: Install dependencies
      #   run: |
      #     npm ci

      # - name: Run lint
      #   run: |
      #     npm run lint

      # - name: Run tests
      #   run: |
      #     npm test -- --watchAll=false

      - name: Temporary success step
        run: echo "✅ Frontend CI placeholder passed successfully!"
